default:
  - 'dev'
build-dev:
  - 'clean:build'
  - 'copy:build'
  - 'sass:build'
  - 'postcss:build'
  - 'coffee:build'
build-dist:
  - 'clean:build'
  - 'copy:build'
  - 'sass:build'
  - 'postcss:build'
  - 'coffee:build'
  - 'cssmin:build'
  - 'compress:plugin'
dev:
  - 'build-dev'
  - 'concurrent:dev'