default:
  - 'dev'
build-dev:
  - 'clean:build'
  - 'copy:build'
  - 'sass:build'
  - 'coffee:build'
build-dist:
  - 'clean:build'
  - 'copy:build'
  - 'sass:build'
  - 'postcss'
  - 'coffee:build'
  - 'cssmin:build'
  - 'compress:plugin'
dev:
  - 'build-dev'
  - 'concurrent:dev'